# Chatbot Backend

This backend handles chatbot requests using OpenAI API.

## Setup
Install dependencies:
npm install

Run the server:
npm start

Add your API key in Render under Environment Variables:
OPENAI_API_KEY=your_key
